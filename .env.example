# ===== Server Configuration =====

#===== External API Configuration =====
# ITAD (IsThereAnyDeal) API Configuration
ITAD_API_KEY=your_itad_api_key_here
ITAD_CLIENT_ID=your_itad_client_id_here
ITAD_CLIENT_SECRET=your_itad_client_secret_here
# Steam API Key
STEAM_API_KEY=your_steam_api_key_here

# JWT Secret Key (MUST BE CHANGED IN PRODUCTION!)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-secret-key-here-min-32-bytes

# JWT Token Time-To-Live in seconds (default: 1200 = 20 minutes)
JWT_TTL_SECONDS=1200

# Client Credentials (JSON format)
# Format: {"client_id": "client_secret", ...}
# Generate secret with: python -c "import secrets; print(secrets.token_urlsafe(32))"
CLIENTS_JSON={"desktop-main": "your-client-secret-here"}

# ===== Client Configuration =====

# Client ID (must match one in CLIENTS_JSON above)
CLIENT_ID=desktop-main

# Client Secret (must match the secret for CLIENT_ID in CLIENTS_JSON)
CLIENT_SECRET=your-client-secret-here

# Server URL (where the FastAPI server is running)
SERVER_URL=http://localhost:8000
HOST=localhost
PORT=8000

# ===== Database Configuration =====
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password_here
PGDATABASE=postgres
